import { SiteLink } from "@/components/links/SiteLink";
import { SiteLinks } from "@/data/SiteLinks";

all group members must submit contributions statement at the end of the slides for this milestone
<SiteLink linkKey={SiteLinks.assignmentsContributionStatements} outerComponent="span">Contribution Statement</SiteLink>.
